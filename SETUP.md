## Настройка почты

1. Скопируйте `.env.example` в `.env`
2. Настройте переменные:
    - `EMAIL_USERNAME` - email для отправки
    - `EMAIL_PASSWORD` - пароль приложения (не основной пароль!)
3. Для Gmail: включите 2FA и создайте App Password

```bash
cp .env.example .env
```