spring:
  data:
    redis:
      port: 6379
      host: localhost
      channel:
        follower: follower_channel
        achievement: achievement_channel
        comment_event: comment_event_channel

  mail:
    host: smtp.gmail.com
    port: 587
    username: a.v.barakhvostov@gmail.com    #your_email@gmail.com #https://www.baeldung.com/spring-email
    password: sgtb yeiy aagq fxhi   #whatever #генерируем для своего Google аккаунта по инструкции https://support.google.com/accounts/answer/185833
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
          connectiontimeout: 5000
          timeout: 5000
          writetimeout: 5000
        debug: true
    protocol: smtp
  messages:
    basename: messages

server:
  port: 8083

logging:
  level:
    root: info

sms_service:
  mts_exolve_api:
    sms_uri: https://api.exolve.ru/messaging/v1/SendSMS
    headers:
      auth_key: Authorization
      auth_prefix: Bearer
    params:
      keys:
        sender_key: number
        receiver_key: destination
        message_key: text
      values:
        api_key: some_api_key
        sender_number: some_sender_number
  retryable:
    max_attempts: 2
    delay: 10000

project-service:
    host: localhost
    port: 8082
user-service:
    host: localhost
    port: 8080
    path: "user-service"
