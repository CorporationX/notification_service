spring:
  data:
    redis:
      port: 6379
      host: localhost
      topic: "follower_topic"
      channel:
        follower: follower_channel
        achievement: achievement_channel
  kafka:
        bootstrap-servers: localhost:9092
        topics:
          mentorship-request: mentorship-request-topic
          achievement: user_achievement
        consumer:
          group-id: notification-service-group
          auto-offset-reset: earliest
          key-deserializer: org.apache.kafka.common.serialization.StringDeserializer
          value-deserializer: org.springframework.kafka.support.serializer.JsonDeserializer
          properties:
            spring.json.trusted.packages: "faang.school.notificationservice.dto"
  mail:
    host: smtp.gmail.com
    port: 587
    username: corporationx.bot@gmail.com
    password: rzmcyykwdhhfppoa
    debug: true
    transport:
      protocol: true
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
          connectiontimeout: 5000
          timeout: 5000
          writetimeout: 5000

  messages:
    basename: messages

server:
  port: 8083

logging:
  level:
    root: info
    feign: DEBUG
    feign.Logger: WARN
    feign.hystrix: WARN

    org:
      springframework:
        cloud:
          openfeign: DEBUG

    org.apache.http: DEBUG
    okhttp3: DEBUG

    feign.template: OFF

vonage:
  api:
    key: whatever
    secret: whatever

project-service:
  host: localhost
  port: 8082

user-service:
  host: localhost
  port: 8080

sms:
  url: "http://api.prostor-sms.ru/"
  username: "t89994676160"
  password: "504421"
  
telegram:
  chat: -1002709076416L
  bot:
    username: CorporationX_kraken_bot
    token: 7854027302:AAEaW444lQwgkW6UkC9cB5kgHvIf_0ShToc
    enable: true
