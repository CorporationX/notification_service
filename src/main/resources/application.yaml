spring:
  data:
    redis:
      port: 6379
      host: localhost
      jedis:
        pool:
          max-idle: 5
          max-active: 10
          min-idle: 1
          max-wait: 1000
      listener:
        thread-pool:
          core-size: 4
          max-size: 8
          queue-capacity: 100
          thread-name-prefix: redis-listener-
      channel:
        follower: follower_channel
        achievement: achievement_channel
        like: like_channel
        not-like: not_like_channel

  mail:
    host: smtp.gmail.com
    port: 587
    username: vcpspto@gmail.com #https://www.baeldung.com/spring-email
    password: anbi qeqw coev rwnj #генерируем для своего Google аккаунта по инструкции https://support.google.com/accounts/answer/185833
    from: vcpspto@gmail.com
    replyTo: vcpspto@gmail.com
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
          connectiontimeout: 5000
          timeout: 5000
          writetimeout: 5000
    retry:
      max-attempts: 3
      backoff: 1000
      multiplier: 2

  messages:
    basename: messages

server:
  host: localhost
  port: 8083

logging:
  level:
    root: info

#Можно зарегистрировать аккаунт без привязки карты. Будет 2 евро на отправку тестовых SMS
vonage: #https://dashboard.nexmo.com/getting-started/sms
  api:
    key: ea3cbdef
    secret: t6qqwTma9etW5JwR

sms:
  provider: vonage

project-service:
    host: localhost
    port: 8082
user-service:
    host: localhost
    port: 8080

telegram:
  bot-username: MyRamilNotifierBot
  bot-token: 8081925035:AAEWp8cxBNRVcnzlvg7nJxJax4NTsQFSlDU

executor:
  email:
    thread-core-size: 8
    max-pool-size: 16
    queue-capacity: 50

