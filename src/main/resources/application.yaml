spring:
  kafka:
    bootstrap-servers: localhost:9092
    replicas: 1
    consumer:
      group-id: notification-group
      auto-offset-reset: earliest
      key-deserializer: org.apache.kafka.common.serialization.StringDeserializer
      value-deserializer: org.springframework.kafka.support.serializer.JsonDeserializer
      properties:
        spring.json.trusted.packages: "faang.school.notificationservice.event"
      comment-create:
        topic: "comment-create"
  data:
    redis:
      port: 6379
      host: localhost
      channel:
        follower: follower_channel
        achievement: achievement_channel

  mail:
    host: smtp.gmail.com
    port: 587
    username: krakenteam64@gmail.com #https://www.baeldung.com/spring-email
    password: qfpe ykgz yvmn mjzq
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
          connectiontimeout: 5000
          timeout: 5000
          writetimeout: 5000

  messages:
    basename: messages

server:
  port: 8083

logging:
  level:
    root: info

#Можно зарегистрировать аккаунт без привязки карты. Будет 2 евро на отправку тестовых SMS
vonage: #https://dashboard.nexmo.com/getting-started/sms
  api:
    key: ea3cbdef
    secret: t6qqwTma9etW5JwR
    from: Vonage Api

project-service:
    host: localhost
    port: 8082
user-service:
    host: localhost
    port: 8080
    servlet_path: "/api/v1"

telegram:
  bot_username: "Corporation_XBot"
  botToken: "7686886258:AAGpaNweT6fE6PB7_p6uXIsF8L8ogGLFd5A"