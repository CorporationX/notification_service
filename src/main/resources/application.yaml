spring:
  kafka:
    bootstrap-servers: localhost:9092
    consumer:
      group-id: user-service-group
    topics:
      goal-completed-topic:
        name: goal-completed
      subscription:
        new-follower-topic:
          name: new-follower-events
        unfollow-topic:
          name: unfollow-events
      profile:
        name: view-profile
      recover:
        name: transfer-events-dlt
      event-start-topic:
        name: event-start
      comment-created-topic:
        name: comment-created-notification
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://localhost:5432/postgres
    username: user
    password: password
  jpa:
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    hibernate:
      ddl-auto: none
    show-sql: false
    properties:
      hibernate:
        format_sql: true

  liquibase:
    enabled: true
    change-log: classpath:db/changelog/db.changelog-master.yaml

  data:
    redis:
      port: 6379
      host: localhost
      channel:
        follower: follower_channel
        achievement: achievement_channel

  mail:
    host: smtp.gmail.com
    port: 587
    username: corpxbootcampnotifier@gmail.com
    password: cjrg bqse jxlb jooo
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
          connectiontimeout: 5000
          timeout: 5000
          writetimeout: 5000

  messages:
    basename: messages

server:
  port: 8083

logging:
  level:
    root: info

#Можно зарегистрировать аккаунт без привязки карты. Будет 2 евро на отправку тестовых SMS
vonage: #https://dashboard.nexmo.com/getting-started/sms
  api:
    key: whatever
    secret: whatever

exolve:
  sms:
    uri: https://api.exolve.ru/messaging/v1/SendSMS
    service-number: 79584811889
  api:
    key: whatever

telegram:
  bot:
    username: CorpXBootCampNotifierBot
    token: 7880406726:AAFNCK30XewGxlej-HLjshfRrUenPsD5cSQ
    link-template: "https://t.me/${telegram.bot.username}?start=%s"

project-service:
    host: localhost
    port: 8082
user-service:
    host: localhost
    port: 8080
